services:
  # backend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./backend:/app
  #   environment:
  #     - FLASK_APP=app.py
  #     - FLASK_ENV=development
  #   depends_on:
  #     - db
  #   networks:
  #     - ros-net

  db:
    image: postgres:16
    volumes:
      - postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: react-ros-ui
    ports:
      - "5432:5432"
    networks:
      - ros-net

volumes:
  postgres-data:

networks:
  ros-net:
    driver: bridge